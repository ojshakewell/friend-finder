//submitted responces loop friends array and compare each scores
//to the submitted

	//iderate through submitted subttact scores(absolute vaue)
	//push each result to array temp 
	//add array total to another object with key value pair with name
	//add each object to array check which has lowest value return the key of the oject
	//return vaule name check the friends array return object that matches
